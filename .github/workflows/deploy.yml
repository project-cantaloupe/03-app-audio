name: CI Pipeline for app-audio

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

env:
  HARBOR_REGISTRY: cntlp-onp-wk-01.tail270b85.ts.net:31432
  HARBOR_PROJECT: library

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1. 저장소 소스코드 체크아웃
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2. Tailscale VPN 연결
      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          tags: tag:ci
          hostname: github-runner-${{ github.run_id }}

      # 3. Docker 설정 및 Harbor 로그인
      - name: Configure Docker & Login to Harbor
        env:
          USERNAME: ${{ secrets.HARBOR_USERNAME }}
          PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
        run: |
          # 1) Tailscale IP 사전 추출 후 /etc/hosts 강제 등록 (DNS 지원)
          HARBOR_IP=$(getent hosts cntlp-onp-wk-01.tail270b85.ts.net | awk '{ print $1 }')
          echo "Resolved Harbor IP: $HARBOR_IP"

          if [ -n "$HARBOR_IP" ]; then
            echo "$HARBOR_IP cntlp-onp-wk-01.tail270b85.ts.net" | sudo tee -a /etc/hosts
          fi

          # 2) Insecure Registry 설정 및 도커 재시작
          sudo mkdir -p /etc/docker
          cat <<EOF | sudo tee /etc/docker/daemon.json
          {
            "insecure-registries": [
              "${{ env.HARBOR_REGISTRY }}"
            ]
          }
          EOF

          sudo systemctl restart docker

          # 3) Harbor 네트워크 연결 가용성 확인
          echo "Waiting for Harbor..."
          for i in {1..10}; do
            if curl -s --connect-timeout 2 http://${{ env.HARBOR_REGISTRY }}/v2/ >/dev/null; then
              echo "Harbor network is ready!"
              break
            fi
            echo "Retry $i..."
            sleep 3
          done

          # 4) 개행 문자 유실 없는 안정적인 Harbor 로그인
          printf '%s' "$PASSWORD" | docker login \
            "${{ env.HARBOR_REGISTRY }}" \
            --username "$USERNAME" \
            --password-stdin

      # 4. API 서비스 빌드 및 푸시
      - name: Build and Push API Service
        run: |
          docker build \
            -t ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-api:latest \
            -t ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-api:${{ github.sha }} \
            -f ./services/api/Dockerfile ./services/api

          docker push ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-api --all-tags

      # 5. Web 서비스 빌드 및 푸시
      - name: Build and Push Web Service
        run: |
          docker build \
            --build-arg VITE_DEV_SUBJECT="${{ vars.VITE_DEV_SUBJECT }}" \
            --build-arg VITE_AUTH_MODE="${{ vars.VITE_AUTH_MODE || 'development' }}" \
            --build-arg VITE_API_BASE_URL="${{ vars.VITE_API_BASE_URL }}" \
            -t ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-web:latest \
            -t ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-web:${{ github.sha }} \
            -f ./services/web/Dockerfile ./services/web

          docker push ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-web --all-tags

      # 6. Worker 서비스 빌드 및 푸시
      - name: Build and Push Worker Service
        run: |
          docker build \
            -t ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-worker:latest \
            -t ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-worker:${{ github.sha }} \
            -f ./services/worker/Dockerfile ./services/worker

          docker push ${{ env.HARBOR_REGISTRY }}/${{ env.HARBOR_PROJECT }}/app-audio-worker --all-tags
